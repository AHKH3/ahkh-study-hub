# Public YouTube Innertube client key (reads public caption tracks only).
# Any key shipped inside YouTube's own clients works; the default below is the
# well-known public one also used by tools like yt-dlp. Never commit a real
# private key here. CI receives this value through the YOUTUBE_INNERTUBE_KEY
# repository secret instead.
YOUTUBE_INNERTUBE_KEY=
