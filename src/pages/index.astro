---
import BaseLayout from '../layouts/BaseLayout.astro';
import HubHeader from '../components/HubHeader.astro';
import { COURSES, type CourseStatus } from '../data/courses';
import { path } from '../utils/paths';
import { getCategoryColor } from '../utils/categoryColors';
import { countLessons } from '../utils/courseStats';

const STATUS_CONFIG: Record<CourseStatus, { label: string; dotClass: string; textClass: string }> = {
  active: {
    label: 'Active',
    dotClass: 'bg-emerald-600 dark:bg-emerald-400',
    textClass: 'text-emerald-700 dark:text-emerald-400',
  },
  new: {
    label: 'New',
    dotClass: 'bg-blue-600 dark:bg-blue-400',
    textClass: 'text-blue-700 dark:text-blue-400',
  },
  explored: {
    label: 'Explored',
    dotClass: 'bg-purple-600 dark:bg-purple-400',
    textClass: 'text-purple-700 dark:text-purple-400',
  },
  completed: {
    label: 'Completed',
    dotClass: 'bg-pink-600 dark:bg-pink-400',
    textClass: 'text-pink-700 dark:text-pink-400',
  },
};
---

<BaseLayout title="Library — AHKH Study Hub" description="Curated library of intensive study curricula and open course companions.">
  <HubHeader activeTab="library" />

  <main class="flex-1 max-w-reading w-full mx-auto px-6 py-12">
    <!-- Header Section -->
    <div class="mb-10 flex items-baseline justify-between gap-4">
      <h1 class="font-serif text-3xl sm:text-4xl font-medium tracking-tight text-ink dark:text-dark-ink">
        Index of Curricula
      </h1>
      <span class="font-mono text-xs text-ink-muted dark:text-dark-muted shrink-0">
        0{COURSES.length} tracks
      </span>
    </div>

    <!-- The Editorial Monograph List -->
    <div class="divide-y divide-ink-border/80 dark:divide-dark-border border-t border-b border-ink-border dark:border-dark-border mb-16">
      {COURSES.map((course, index) => {
        const num = String(index + 1).padStart(2, '0');
        const statusMeta = STATUS_CONFIG[course.status];
        return (
          <article class="group py-5 sm:py-6 transition-colors hover:bg-paper-200/40 dark:hover:bg-dark-card/60 px-2 sm:px-4 -mx-2 sm:-mx-4 rounded-xs" data-course-id={course.id} data-lessons={course.modules.flatMap((m) => m.lessons.map((l) => l.slug)).join(',')}>
            <a href={path(`/courses/${course.slug}`)} class="block focus:outline-hidden">
              <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-4">
                
                <!-- Left: Number & Course Info -->
                <div class="flex items-start sm:items-center gap-4 sm:gap-6 min-w-0">
                  <span class="font-mono text-xs sm:text-sm text-ink-muted dark:text-dark-muted font-medium shrink-0 pt-0.5 sm:pt-0">
                    {num}
                  </span>
                  <div>
                    <div class="mb-1">
                      <span class={`font-mono text-[10px] sm:text-[11px] uppercase tracking-wider font-medium ${getCategoryColor(course.category)}`}>
                        {course.category}
                      </span>
                    </div>
                    <div class="flex flex-wrap items-center gap-3">
                      <h2 class="font-serif text-xl sm:text-2xl font-medium text-ink dark:text-dark-ink group-hover:text-black dark:group-hover:text-white group-hover:underline transition-colors">
                        {course.title}
                      </h2>
                      {statusMeta && (
                        <span class={`inline-flex items-center gap-1.5 text-[11px] font-mono tracking-wide ${statusMeta.textClass}`} data-statusbadge>
                          <span class={`w-1.5 h-1.5 rounded-full ${statusMeta.dotClass}`} data-statusdot></span>
                          {statusMeta.label}
                        </span>
                      )}
                    </div>
                    <!-- Metadata metrics: Module count, Source count, Duration -->
                    <div class="flex items-center gap-2.5 text-xs font-mono text-ink-muted/80 dark:text-dark-muted/80 mt-1.5">
                      <span><span class="font-medium text-ink dark:text-dark-ink">{course.modules.length}</span> Modules</span>
                      <span>·</span>
                      <span><span class="font-medium text-ink dark:text-dark-ink">{countLessons(course)}</span> Sources</span>
                      {course.duration && (
                        <>
                          <span>·</span>
                          <span class="font-medium text-ink dark:text-dark-ink">{course.duration}</span>
                        </>
                      )}
                    </div>
                  </div>
                </div>

                <!-- Right: Progress Indicator -->
                <div class="flex items-center shrink-0 pl-8 sm:pl-0">
                  <div class="flex items-center gap-2 font-mono text-xs text-ink dark:text-dark-ink">
                    <div class="w-24 sm:w-28 h-1.5 bg-paper-300 dark:bg-dark-border rounded-full overflow-hidden">
                      <div 
                        class="h-full bg-ink dark:bg-dark-ink transition-all progress-fill"
                        data-role="progress-fill"
                        style={`width: ${course.progressPercent}%`}
                      ></div>
                    </div>
                    <span class="w-9 text-right font-medium progress-label">{course.progressPercent}%</span>
                  </div>
                </div>

              </div>
            </a>
          </article>
        );
      })}
    </div>

    <!-- Editorial Colophon Footer -->
    <footer class="pt-8 border-t border-ink-border/60 dark:border-dark-border flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4 text-xs font-sans text-ink-muted dark:text-dark-muted">
      <p>A sovereign, local-first reading sanctuary for rigorous study curricula.</p>
      <a href={path('/manifesto')} class="hover:text-ink dark:hover:text-dark-ink transition-colors underline underline-offset-4">
        Manifesto &rarr;
      </a>
    </footer>
  </main>

  <script is:inline>
    const STATUS_META = {
      active: { label: 'Active', dot: 'bg-emerald-600 dark:bg-emerald-400', text: 'text-emerald-700 dark:text-emerald-400' },
      new: { label: 'New', dot: 'bg-blue-600 dark:bg-blue-400', text: 'text-blue-700 dark:text-blue-400' },
      explored: { label: 'Explored', dot: 'bg-purple-600 dark:bg-purple-400', text: 'text-purple-700 dark:text-purple-400' },
      completed: { label: 'Completed', dot: 'bg-pink-600 dark:bg-pink-400', text: 'text-pink-700 dark:text-pink-400' }
    };
    function refreshLibraryProgress() {
      try {
        if (typeof localStorage === 'undefined') return;
        const articles = document.querySelectorAll('article[data-course-id]');
        articles.forEach((article) => {
          try {
            const courseId = article.getAttribute('data-course-id');
            const lessonsAttr = article.getAttribute('data-lessons') || '';
            const slugs = lessonsAttr ? lessonsAttr.split(',').filter(Boolean) : [];
            const total = slugs.length;
            let read = 0;
            try {
              slugs.forEach((slug) => {
                if (localStorage.getItem(`ahkh_read_${courseId}_${slug}`) !== null) read += 1;
              });
            } catch (e) {}
            const percent = total ? Math.round((read / total) * 100) : 0;
            const fill = article.querySelector('[data-role="progress-fill"]');
            if (fill) fill.style.width = percent + '%';
            const label = article.querySelector('.progress-label');
            if (label) label.textContent = percent + '%';
            if (percent === 100) {
              const badge = article.querySelector('[data-statusbadge]');
              const meta = STATUS_META.completed;
              if (badge && meta) {
                const dot = badge.querySelector('[data-statusdot]');
                if (dot) dot.className = `w-1.5 h-1.5 rounded-full ${meta.dot}`;
                ['text-emerald-700', 'dark:text-emerald-400', 'text-blue-700', 'dark:text-blue-400', 'text-purple-700', 'dark:text-purple-400', 'text-pink-700', 'dark:text-pink-400'].forEach((c) => badge.classList.remove(c));
                meta.text.split(' ').forEach((c) => badge.classList.add(c));
                const textNode = Array.from(badge.childNodes).find((n) => n.nodeType === 3 && n.textContent.trim() !== '');
                if (textNode) textNode.textContent = ` ${meta.label}`;
                else badge.append(document.createTextNode(` ${meta.label}`));
              }
            }
          } catch (e) {}
        });
      } catch (e) {}
    }
    refreshLibraryProgress();
    if (!window.__ahkhLibBoot) {
      window.__ahkhLibBoot = true;
      document.addEventListener('astro:page-load', refreshLibraryProgress);
    }
  </script>
</BaseLayout>
